<h1><%= @doctor.name %> </h1>
<% @doctor.appointments.each do |appt| %>
    <li>Time: <%= appt.user_friendly_time(appt.appointment_datetime)%></li>
    <li>Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
    <%end%>
  
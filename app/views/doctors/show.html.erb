<h1>Doctors#show</h1>
<p>Find me in app/views/doctors/show.html.erb</p>
<% @doctor.appointments.each do |a| %>
<%= link_to a.patient.name, patient_path(a.patient)%>
<%= a.appointment_datetime.strftime('%B %e, %Y at %H:%M')%><br>
<% end %>

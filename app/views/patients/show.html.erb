<h1> This patient's appointments: </h1>

<ul>
  <% @patient.appointments.each do |appt| %>
    <li><%= appt.appointment_datetime.strftime("%B %-d, %Y at %H:%M") %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> </li>
  <% end %>
</ul>